<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= RESTAURANT.name %> - <%= category.charAt(0).toUpperCase() + category.slice(1) %>  %></title>
</head>
<body>
    <%- include('./partials/nav') %>
   
  <h1 class="top"><%= category.charAt(0).toUpperCase() + category.slice(1) %> Menu</h1>
    <ul>
     <% menu.forEach(item => { %>
        <li>
           <h2 class="item"><%= item.name %></h2>
           <p class="item"><%= item.details %></p>
           <p class="item">Price: $<%=item.price %>
           <p class="item">Rating: <%= item.rating %></p>
        </li>
      <% }) %>
    </ul>
</body>
</html>


<style>
  
.top {
  display: flex;
  justify-content: center;
}

h2 {
  font-size: 35PX;
}

p {
  font-size:25px;
}

.item {
  display: flex;
  justify-content: center;
}

li {
  list-style: none;
}

</style>